[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "python3", "yt-dlp"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "mkdir -p temp public/temp/frames"
]

[start]
cmd = "sh -c 'node_modules/.bin/next start -p ${PORT:-3000}'"
